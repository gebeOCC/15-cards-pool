import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '15-cards-pool',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
